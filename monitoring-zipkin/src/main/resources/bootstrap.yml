spring:
  application:
    name: monitoring-zipkin
  cloud:
    config:
      uri: http://config:8888
      fail-fast: true
      password: ${CONFIG_SERVICE_PASSWORD:c123456}
      username: user
      enabled: true