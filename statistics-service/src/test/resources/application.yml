hystrix:
  command:
    default:
      execution:
        isolation:
          thread:
            timeoutInMilliseconds: 20000

spring:
  data:
    mongodb:
      database: piggymetrics
      port: 0
  http:
    converters:
      preferred-json-mapper: jackson

rates:
  url: http://api.fixer.io
